package request.accountMenuRequest;

public enum AccountError {
    USERNAME_ALREADY_EXIST,
    USERNAME_DOESENT_EXIST,
    PASSWORD_IS_INCORRECT,
    ALREADY_LOGGED_IN,
    FIELDS_CAN_NOT_BE_EMPTY
}
